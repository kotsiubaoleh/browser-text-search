{
  "dependencies": {
    "@assemblyscript/loader": "^0.19.16",
    "static-server": "^2.2.1"
  },
  "scripts": {
    "start": "cd public && static-server",
    "asbuild:untouched": "asc assembly/index.ts --target debug",
    "asbuild:optimized": "asc assembly/index.ts --target release",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized && cp build/optimized.wasm public/optimized.wasm",
    "test": "node tests"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.16"
  }
}